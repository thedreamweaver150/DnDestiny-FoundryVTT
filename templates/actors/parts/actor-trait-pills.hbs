{{#if (or @root.editable values.length)}}
<div class="pills-group {{#unless values.length}}empty{{/unless}}">
    <h3 class="icon">
        {{#if icon}}<i class="{{ icon }}"></i>
        {{~else if svg}}<dndestiny-icon src="systems/dndestiny/icons/svg/{{ svg }}.svg"></dndestiny-icon>{{/if}}
        <span class="roboto-upper">{{ localize label }}</span>
        {{#if (and @root.editable (or configAction key))}}
        <button type="button" class="config-button unbutton"
                {{~#if configAction}} data-action="showConfiguration" data-config="{{ configAction }}"
                {{~else}} data-action="showConfiguration" data-trait="{{ key }}"{{/if}}
                data-tooltip aria-label="{{ localize 'DND5E.TraitConfig' trait=(localize label) }}">
            <i class="fas fa-cog" inert></i>
        </button>
        {{/if}}
    </h3>
    <ul class="pills">
        {{#each values}}
        <li class="pill {{ ../color }} {{ color }} {{~ifThen draggable ' draggable' ''}}">
            {{#if link}}<a class="{{ @root.rollableClass }}" {{ dndestiny-dataset link }}>{{/if}}
            {{#each icons}}
            <i class="{{ icon }}" data-tooltip="{{ label }}" aria-label="{{ label }}"></i>
            {{/each}}
            <span class="label">{{ label }}</span>
            {{#if value}}
            <span class="separator">&vert;</span>
            <span class="value">{{ dndestiny-numberFormat value signDisplay=../signDisplay }}</span>
            {{/if}}
            {{#if link}}</a>{{/if}}
        </li>
        {{/each}}
    </ul>
</div>
{{/if}}
