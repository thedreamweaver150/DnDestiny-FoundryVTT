<fieldset>
    <legend>Shield Die</legend>
    <div class="form-group">
        <label>Shield Die</label>
        <span class="form-field-readonly">{{ shieldDie }}</span>
    </div>
    <div class="form-group">
        <label>Average</label>
        <span class="form-field-readonly">{{ advancement.average }}</span>
    </div>
</fieldset>
