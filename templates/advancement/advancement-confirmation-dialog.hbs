<section class="flexcol">
    <p class="note warn">{{ content }}</p>
    <fieldset>
        <legend>{{ localize "DND5E.ADVANCEMENT.Deletion.Options" }}</legend>
        <div class="form-group">
            <label for="{{ partId }}-apply">{{ localize "DND5E.ADVANCEMENT.Deletion.Label" }}</label>
            <div class="form-fields">
                <dndestiny-checkbox id="{{ partId }}-apply" name="apply-advancement" checked></dndestiny-checkbox>
            </div>
        </div>
    </fieldset>
</section>
